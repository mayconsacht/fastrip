<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :category %><br>
      <%= f.text_field :category %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :origin %><br>
      <%= f.text_field :origin %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :destination %><br>
      <%= f.text_field :destination %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :price %><br>
      <%= f.text_field :price %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :in_date %><br>
      <%= f.date_select :in_date, min  %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :out_date %><br>
      <%= f.date_select :out_date %>
    </div>
  </div>

  <div class="row control-group">
    <div class="field col-xs-4 form-group controls">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

    <%= link_to "Cadastre-se!", new_user_registration_path, :class => 'btn btn-outline btn-white bt-lg'  %> 


<% end %>
